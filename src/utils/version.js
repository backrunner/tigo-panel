/* eslint-disable no-console */
import { version } from '../common/version';

console.log(`%ctigo-panel - v${version} by tigo.js`, 'color:#f16d41;');
console.log(`%cEnvironment: ${process.env.NODE_ENV}`, 'color:#f16d41;');
